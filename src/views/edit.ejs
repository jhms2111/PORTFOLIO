<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

    <link rel="stylesheet" href="css/style.css">

</head>
<body>
    
    <h1><%= title %></h1>

    <%- include('partials/navbar.ejs')%>

    <%  if (typeof message !== 'undefined') { %>
    <%= message %> 
    <% } %> 


   <form action="/edit/<%= user._id %>" method="POST">
      <div><label for="">Nombre</label>
         <input type="text/css" name="name" placeholder="Escribe tu nombre" value="<%= user.name %>">
      </div>
      <div><label for="">Teléfono</label>
         <input type="text/css" name="phone" placeholder="Escribe tu teléfono"value="<%= user.phone %>">
      </div>
      <div><label for="">E-mail</label>
         <input type="text/css" name="email" placeholder="Escribe tu teléfono" value="<%= user.email %>">
      </div>
      <div><label for="">Mensaje</label>
         <input type="text/css" name="mensaje" placeholder="Digite o seu email " value="<%= user.mensaje %>">
      </div>
      <button type="submit">Editar</button>
   </form>
     </div>
 

   <script src="js/main.js"></script>

</body>
</html>